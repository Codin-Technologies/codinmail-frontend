import { AdminConsole } from '@/app/components/admin/admin-console';

export const metadata = { title: 'Organization Admin | Codin Mail', description: 'Manage organization identity, people, domains, security, and platform operations.' };

export default function AdminPage() {
  return <AdminConsole />;
}
